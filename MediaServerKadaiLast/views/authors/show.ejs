<h2 class="page-header"><%= author.name %></h2>
<div class="btn-row">
    <a class="btn btn-primary" href="/authors/<%= author.id %>/edit">編集</a>
    <%- include('../partials/deleteForm', { url: `/authors/${author.id}` }) %>
</div>
<% if(booksByAuthor.length > 0) { %>
    <h2 class="page-header">アップローダーによる画像</h2>
    <%- include('../partials/bookGrid', { books: booksByAuthor}) %>
<% } %>